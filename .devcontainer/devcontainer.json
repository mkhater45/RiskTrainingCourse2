{
  "name": "Python & SQL Risk Analytics Lab",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "jupyter.interactiveWindow.intro.show": false
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "qwtel.sqlite-viewer",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-sqlite"
      ]
    }
  },

  // 1. Installs data stack (pandas, numpy, matplotlib)
  // 2. Automatically pulls and hydrates the IBM AML SQLite DB on launch
  "postCreateCommand": "pip install --upgrade pip && pip install pandas numpy matplotlib && mkdir -p data && curl -L -o data/bank_risk_aml.zip https://github.com/SuccinctGasca/RiskTrainingCourse2/releases/download/1.0.0/bank_risk_aml_medium.zip && unzip data/bank_risk_aml.zip -d data/ && rm data/bank_risk_aml.zip"
}
